name: dropsource_utils
description: A set of useful functions and libraries used in almost all
  Dropsource flutter apps.
version: 0.0.3+4
author: Nathan Frechette<nate@dropsource.com>
homepage: https://dropsource.com

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^3.0.10
  firebase_core: ^0.5.0
  firebase_analytics: ^6.0.0
  firebase_crashlytics: ^0.2.0
  rxdart: ^0.24.1
  provider: ^4.3.2+2
  auto_size_text: ^2.1.0
  build_context: ^2.0.4
  intl: ^0.16.1
  url_launcher: ^5.7.2
  flutter_email_sender: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
