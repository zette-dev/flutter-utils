name: dropsource_ui
description: A set of useful functions and libraries used in almost all
  Dropsource flutter apps.
version: 0.0.4+1
homepage: https://dropsource.com
publish_to: none

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  auto_size_text: ^3.0.0
  build_context: ^3.0.0
  connectivity_plus: ^2.2.1
  dio: ^4.0.4
  dropsource_core:
    git:
      url: https://github.com/Dropsource/flutter_utils.git
      path: core
      ref: riverpod
  firebase_analytics: ^9.1.3
  firebase_core: ^1.14.0
  firebase_crashlytics: ^2.6.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^1.0.3
  go_router: ^3.0.6
  logging: ^1.0.2
  url_launcher: ^6.0.20

dev_dependencies:
  flutter_test:
    sdk: flutter
